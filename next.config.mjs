/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "png.pngtree.com",
      "upload.wikimedia.org",
      "i.imgur.com",
      "sekarbumi.com",
      "placehold.co",
      "cdn.dummyjson.com",
      "via.placeholder.com",
    ],
  },
};

export default nextConfig;
