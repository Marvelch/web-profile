/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "png.pngtree.com",
      "upload.wikimedia.org",
      "i.imgur.com",
      "sekarbumi.com",
      "cdn.projects.co.id",
    ],
  },
};

export default nextConfig;
